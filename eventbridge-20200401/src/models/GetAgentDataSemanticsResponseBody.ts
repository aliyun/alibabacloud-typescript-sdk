// This file is auto-generated, don't edit it
import * as $dara from '@darabonba/typescript';
import { AgentDataSemanticsExample } from "./AgentDataSemanticsExample";
import { AgentDataSemanticsJoin } from "./AgentDataSemanticsJoin";
import { AgentDataSemanticsMetric } from "./AgentDataSemanticsMetric";
import { AgentDataSemanticsText } from "./AgentDataSemanticsText";


export class GetAgentDataSemanticsResponseBodyData extends $dara.Model {
  /**
   * @remarks
   * The SQL example knowledge entries. A maximum of 50 entries are supported.
   */
  examples?: AgentDataSemanticsExample[];
  /**
   * @remarks
   * The data association knowledge entries. A maximum of 100 entries are supported.
   */
  joins?: AgentDataSemanticsJoin[];
  /**
   * @remarks
   * The SQL expression knowledge entries. A maximum of 100 entries are supported.
   */
  metrics?: AgentDataSemanticsMetric[];
  /**
   * @remarks
   * The text knowledge in Markdown format.
   */
  text?: AgentDataSemanticsText;
  /**
   * @remarks
   * The currently effective version number.
   * 
   * @example
   * 3
   */
  versionNo?: number;
  static names(): { [key: string]: string } {
    return {
      examples: 'Examples',
      joins: 'Joins',
      metrics: 'Metrics',
      text: 'Text',
      versionNo: 'VersionNo',
    };
  }

  static types(): { [key: string]: any } {
    return {
      examples: { 'type': 'array', 'itemType': AgentDataSemanticsExample },
      joins: { 'type': 'array', 'itemType': AgentDataSemanticsJoin },
      metrics: { 'type': 'array', 'itemType': AgentDataSemanticsMetric },
      text: AgentDataSemanticsText,
      versionNo: 'number',
    };
  }

  validate() {
    if(Array.isArray(this.examples)) {
      $dara.Model.validateArray(this.examples);
    }
    if(Array.isArray(this.joins)) {
      $dara.Model.validateArray(this.joins);
    }
    if(Array.isArray(this.metrics)) {
      $dara.Model.validateArray(this.metrics);
    }
    if(this.text && typeof (this.text as any).validate === 'function') {
      (this.text as any).validate();
    }
    super.validate();
  }

  constructor(map?: { [key: string]: any }) {
    super(map);
  }
}

export class GetAgentDataSemanticsResponseBody extends $dara.Model {
  /**
   * @remarks
   * The response code.
   * 
   * @example
   * Success
   */
  code?: string;
  /**
   * @remarks
   * The details of the currently effective data semantics version. If generation is running, has failed, or has completed but has not been saved, only the official version is returned. No temporary snapshot is returned.
   */
  data?: GetAgentDataSemanticsResponseBodyData;
  /**
   * @remarks
   * The response message. An error message is returned if the request fails.
   * 
   * @example
   * Data semantics not found.
   */
  message?: string;
  /**
   * @remarks
   * The unique request ID generated by Alibaba Cloud.
   * 
   * @example
   * 5DAF96FB-A4DF-548C-B8A1-F2A8D2F4****
   */
  requestId?: string;
  /**
   * @remarks
   * Indicates whether the call was successful.
   * 
   * @example
   * true
   */
  success?: boolean;
  static names(): { [key: string]: string } {
    return {
      code: 'Code',
      data: 'Data',
      message: 'Message',
      requestId: 'RequestId',
      success: 'Success',
    };
  }

  static types(): { [key: string]: any } {
    return {
      code: 'string',
      data: GetAgentDataSemanticsResponseBodyData,
      message: 'string',
      requestId: 'string',
      success: 'boolean',
    };
  }

  validate() {
    if(this.data && typeof (this.data as any).validate === 'function') {
      (this.data as any).validate();
    }
    super.validate();
  }

  constructor(map?: { [key: string]: any }) {
    super(map);
  }
}

